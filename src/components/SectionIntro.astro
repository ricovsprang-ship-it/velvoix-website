---
interface Props {
  eyebrow: string;
  title: string;
  description: string;
  align?: 'left' | 'center';
}

const { eyebrow, title, description, align = 'left' } = Astro.props;
const centered = align === 'center';
---

<div class:list={['max-w-3xl', centered && 'mx-auto text-center']}>
  <p class="eyebrow">{eyebrow}</p>
  <h2 class="mt-4 text-3xl font-extrabold tracking-tight text-slate-950 md:text-4xl">{title}</h2>
  <p class="mt-5 text-lg leading-8 text-slate-600">{description}</p>
</div>
