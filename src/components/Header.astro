---
import type { Locale, NavItem } from '../content/site';
import LanguageSwitcher from './LanguageSwitcher.astro';

interface Props {
  locale: Locale;
  nav: NavItem[];
}

const { locale, nav } = Astro.props;
---

<header class="sticky top-0 z-50 border-b border-slate-200/70 bg-white/82 backdrop-blur-xl">
  <div class="section-shell flex items-center justify-between gap-6 py-4">
    <a href={locale === 'nl' ? '/' : '/en/'} class="text-slate-950">
      <span class="vx-wordmark text-[1.7rem] md:text-[2.05rem]">Velvoix</span>
    </a>

    <nav class="hidden items-center gap-7 text-sm font-semibold lg:flex">
      {nav.map((item) => (
        <a class="nav-link" href={item.href}>{item.label}</a>
      ))}
    </nav>

    <div class="hidden lg:block">
      <LanguageSwitcher locale={locale} />
    </div>

    <details class="relative lg:hidden">
      <summary class="flex cursor-pointer list-none items-center gap-2 rounded-full border border-slate-200 bg-white/92 px-4 py-2 text-sm font-semibold text-slate-800 shadow-sm">
        Menu
      </summary>
      <div class="panel absolute right-0 top-14 w-[18rem] rounded-3xl p-4">
        <div class="mb-4">
          <LanguageSwitcher locale={locale} />
        </div>
        <div class="flex flex-col gap-1">
          {nav.map((item) => (
            <a class="rounded-2xl px-4 py-3 text-sm font-semibold text-slate-700 hover:bg-slate-100" href={item.href}>
              {item.label}
            </a>
          ))}
        </div>
      </div>
    </details>
  </div>
</header>
