{
  "name": "velvoix-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "lint": "npm run verify:locales",
    "build": "astro build",
    "smoke:locales": "node scripts/locale-route-smoke.mjs",
    "verify:locales": "npm run build && npm run smoke:locales",
    "verify": "npm run lint",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.1",
    "astro": "^7.0.5",
    "gsap": "^3.15.0",
    "tailwindcss": "^4.3.1"
  }
}
